{
  "scripts": {
    "dev": "vuepress dev src",
    "build": "vuepress build src",
    "deploy": "yarn build && git add . && git commit -m 'deploy' && git push",
    "lint": "./node_modules/.bin/eslint './src/.vuepress/**/*.{js,vue}'",
    "lint:fix": "./node_modules/.bin/eslint './src/.vuepress/**/*.{js,vue}' --fix"
  },
  "dependencies": {
    "@kawarimidoll/vuepress-plugin-tailwind": "^2.0.0",
    "vue-js-modal": "^2.0.0-rc.6",
    "vuepress": "^1.8.2",
    "vuex": "^3.6.2"
  },
  "devDependencies": {
    "@goy/vuepress-plugin-svg-icons": "^4.2.3",
    "eslint": "^7.25.0",
    "eslint-plugin-vue": "^7.9.0"
  }
}
